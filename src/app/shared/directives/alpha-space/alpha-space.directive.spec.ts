import { AlphaSpaceDirective } from "./alpha-space.directive";


describe('AlphaSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphaSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
